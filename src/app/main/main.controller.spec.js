'use strict';

describe('controllers', function(){
  var scope;

  beforeEach(module('walletAngularjs'));

  beforeEach(inject(function($rootScope) {
    scope = $rootScope.$new();
  }));

  it('basic spec', inject(function($controller) {


  }));
});
